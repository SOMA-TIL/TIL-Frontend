name: "[CD] Deploy to Dev Server"

on:
  push:
    branches: [dev]

permissions:
  contents: write

jobs:
  changes:
    name: Check for changes
    runs-on: ubuntu-latest
    outputs:
      client: ${{steps.filter.outputs.client}}
      admin: ${{steps.filter.outputs.admin}}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          token: ${{ secrets.TOKEN }}
          filters: |
            client:
              - 'TIL-CLIENT/**'
            admin:
              - 'TIL-ADMIN/**'

  til-client-deploy:
    needs: changes
    if: ${{ needs.changes.outputs.client == 'true' }}
    uses: ./.github/workflows/cd-s3-deploy.yml
    secrets: inherit
    with:
      working_dir: TIL-CLIENT
      s3_bucket: ${{ secrets.S3_BUCKET_NAME }} # TODO: Dev Admin S3 Bucket 정보 변경
      cloudfront: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }} # TODO: Dev Admin CloudFront Distribution ID 변경
    env:
      VITE_PROFILE: dev
      VITE_TIL_API_URL: ${{ secrets.DEV_TIL_API_URL }}

  til-admin-deploy:
    needs: changes
    if: ${{ needs.changes.outputs.admin == 'true' }}
    uses: ./.github/workflows/cd-s3-deploy.yml
    secrets: inherit
    with:
      working_dir: TIL-ADMIN
      s3_bucket: ${{ secrets.S3_BUCKET_NAME }} # TODO: Dev Admin S3 Bucket 정보 변경
      cloudfront: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }} # TODO: Dev Admin CloudFront Distribution ID 변경
    env:
      VITE_PROFILE: dev
      VITE_TIL_ADMIN_API_URL: ${{ secrets.DEV_TIL_ADMIN_API_URL }}
